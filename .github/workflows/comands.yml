on:
  workflow_dispatch:

jobs:
  example:
    runs-on: ubuntu-latest
    steps:
      - name: set API key 
        run: echo "API_KEY=12345" >> $GITHUP_ENV
      - name: use API key 
        run: echo "using API key $API_KEY"
